steps:
  # Step 1: Install dependencies using 'npm install'.
  - name: "node:22"
    entrypoint: "npm"
    args: [ "install" ]
    id: "Install Dependencies"

  # Step 2: Build the Nuxt application.
  - name: "node:22"
    entrypoint: "npm"
    args: [ "run", "build" ]
    id: "Run Nuxt Build"

  # Step 3: Deploy, but explicitly tell Cloud Run to check port 3000.
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: "gcloud"
    args:
      - "run"
      - "deploy"
      - "frontend-service"
      - "--source"
      - "."
      - "--platform"
      - "managed"
      - "--region"
      - "us-central1"
      - "--allow-unauthenticated"
      - "--port=3000" # THIS IS THE FINAL FIX
    id: "Deploy Frontend Service"
options:
  logging: CLOUD_LOGGING_ONLY
